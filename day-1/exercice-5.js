// Créer un fichier ˜/mylastname-my-exercices/day-1/exercice-5.js
// Elle devra être prototypée de la manière suivante : my_array_alpha(str)
export const my_array_alpha = (str) => { 

    // Écrire une fonction qui renvoie un array tout les caractères passé en argument.
    var array = [...str];

    // #Indices
    // Retour attendu : Array
    return array;
}
// #Warning
// Split, reverse, charAt, length ... et toutes les autres méthodes dans l'objet String natif sont interdites.  Les push, concat, length ... et toutes les autres méthodes natives pour manipuler un tableau sont interdites.